import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    gap: 15,
    alignItems: 'center',
  },
  counterContainer: {
    marginTop: 20,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 20,
  },
});

export default styles;
